version: '3.1'

services:
  dashboard_db:
    image: docker.io/postgres
    restart: always
    ports:
      - 127.0.0.1:5432:5432
    environment:
      PGUSER: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: iis